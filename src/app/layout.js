import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Providers } from "./providers";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "Kazol Habib | Frontend Developer & Webflow Expert",
  description: "Personal portfolio of Kazol Habib, an elite Frontend Developer and Webflow Expert specializing in building modern, high-performance, and visually stunning web experiences.",
};

export default function RootLayout({ children }) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
      suppressHydrationWarning
    >
      <body className="min-h-full flex flex-col transition-colors duration-400">
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}
